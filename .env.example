# Local development only. Never commit a real .env file.

# Use "proxy" for secure proxy mode or "local" for direct API calls on your machine only.
ENVIRONMENT_MODE=proxy

# Direct local API credentials
GEMINI_API_KEY=your_google_ai_api_key
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your_supabase_anon_key

# Proxy mode
NETLIFY_PROXY_URL=https://radiant-dodol-0c0bca.netlify.app
